<div class="container">
  <h1>Hello new book</h1>
  <%= simple_form_for @book do |f| %>
    <%= f.input :title %>
    <%= f.input :author %>
    <%= f.rich_text_area :rich_summary %>
    <%= f.input :notes %>
    <%= f.input :rating, error: 'Has to be between 0 - 10' %>
    <%= f.input :photo, as: :file %>
    <%= f.button :submit, class: "btn btn-secondary" %>
  <% end %>
</div>

